# Copy to .env.local and fill in. Never commit real values.
# The API key is server-only and must never be exposed to the client (spec §32).

OPENAI_API_KEY=
# Model is configurable on purpose — never hard-code it in application code (spec §3).
OPENAI_MODEL=
NEXT_PUBLIC_SITE_URL=https://louiesakoda.com
